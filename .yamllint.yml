extends: default

ignore: |
  node_modules/
  vendor/
  .venv/

rules:
  comments:
    min-spaces-from-content: 1
  document-start: disable
  line-length:
    max: 120
  truthy:
    check-keys: false

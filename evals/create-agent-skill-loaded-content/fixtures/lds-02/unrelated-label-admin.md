# Unrelated label-administration summary

The repository currently uses the administrative labels `needs-triage`, `blocked`, and `release-note-required`. This summary describes label administration only; it does not define pull-request review behavior.

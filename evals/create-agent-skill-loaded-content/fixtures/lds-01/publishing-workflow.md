# Publishing request

The requested workflow must inspect the current local change scope, create an intentional commit, push the current branch, and open a draft pull request. It is used by repository contributors when they are ready to publish finished local work.

This evaluation is advisory only. Do not run Git commands, publish changes, or contact GitHub.
